Added keyboard controls and physics.

Prerequisites:
sdl2
gforth
gcc

To build the c code:
gcc game.c -o game.exe $(sdl2-config --cflags) $(sdl2-config --libs)

To run the c code:
./game.exe

To run the forth version:
gforth game.fs
